# HTTP mit TLS-Impersonation (Cloudflare-Bypass)
curl_cffi>=0.7.3

# WebSocket mit TLS-Impersonation (Viewer-Verbindung, Phase 2)
rnet>=2.1.0

# Logging
loguru>=0.7.2

# Web-Dashboard (Phase 3)
flask>=3.0.0

# Telegram-Bot (Phase 4, optional)
python-telegram-bot>=21.0
