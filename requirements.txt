# HTTP + WebSocket with Chrome TLS impersonation (Cloudflare bypass).
# The async ws_connect(headers=...) API used by viewer_ws needs >= 0.9.
curl_cffi>=0.9.0

# Logging
loguru>=0.7.2

# Web dashboard
flask>=3.0.0
waitress>=3.0.0  # production WSGI server for the dashboard (falls back to Flask's)

# Telegram bot (phase 4, optional)
python-telegram-bot>=21.0
