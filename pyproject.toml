[project]
name = "kick-channel-points-miner"
version = "0.1.0"
description = "A pure Kick channel-points farming bot"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
